data/raw/
# workflow が金庫をここに展開する（actions/checkout の path: _raw）。
# 手で git add -A したときに、金庫の中身が公開側に入るのを防ぐ。
_raw/
*.zip
__pycache__/
.venv/
.DS_Store
Thumbs.db

# 予約台帳（VirgoB77/kujiraya-aite-yoyaku・private）を Actions が隣に出す場所。
# 手で git add -A したときに、予約台帳の中身がこちらに入るのを防ぐ
_yoyaku/
