<!--
これは docs/突合率.md の**人が書く正本**。build はこのファイルに書かない。

docs/突合率.md は scripts/build.py がここから毎回作る**生成物**。
docs/突合率.md を手で直すと、次の build で黙って消える。**直すならここを直す。**
（前は build が文面ごと書いていて、手で直した docs/突合率.md を毎回戻していた。#43）

{{...}} の所は build が数を入れる。消すと build が止まる（数が黙って消えないように）。
この注釈の塊（<!-- から -->まで）は docs/突合率.md には出ない。
-->
# 突合率

県警CSVの住所を町丁目の区画に突き合わせた結果。
CLAUDE.md 8節の判断基準は「1%未満」。

出すのは**割合**と、**数えなかったものの理由ごとの全体合計**。
市ごと・手口ごと・年ごとの行数は出さない（共通仕様3.2）。
突き合わせできなかった行そのものは、理由つきで金庫に残している。

## 数えなかったもの（`index.json` の `not_counted`）

分けているのは「読めたか」ではなく**何をすれば減るか**。

| 箱 | 件数 | 何をすれば減るか | 中身 |
|---|---:|---|---|
| `unresolved` | {{数えなかったもの.unresolved}} | 語彙を足す | — |
| `unobserved` | {{数えなかったもの.unobserved}} | 取りに行く | 町丁目の欄が空／境界データの版が古く、その版に該当が無い |
| `undecided` | {{数えなかったもの.undecided}} | 元データの書き方が変わる | 丁目の記載が無い／親と丁目が併存し、寄せると推測になる |
| `gone` | {{数えなかったもの.gone}} | 時間が決める | — |

**4つとも出している。0でも出す。** 欠けている鍵は0ではない。

境界データは令和2年国勢調査。**下敷きにするデータにも版がある。**
古い版で引けなかったものは「無い」ではなく「その版には無い」。

{{割合の表}}
